{{-- <div class="ap-header">
    <table class="ap-header-table">
        <tbody>
            <tr>
                <td width="35%">
                    <div class="ap-header-logo">
                        <img src="{{ env('CDN_URL', '') . '/storage/' . $company->logo }}" height="55" width="55">
                    </div>
                    <div class="ap-header-logo">
                        <img src="{{ env('CDN_URL', '') . '/storage/' . $company->sub_logo }}" height="55" width="55">
                    </div>
                </td>

                <td width="65%">
                    <p class="ap-header-name">{{ $idSetting->name }}</p>
                    <p class="ap-header-title">{{$content->title}}</p>
                </td>
            </tr>
        </tbody>
    </table>

    <hr>
</div> --}}


